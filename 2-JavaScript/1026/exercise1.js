
var arr = [1,2,3,4];

var funeach = forEach(arr, fun=>{
console.log(arr);
})

arr.funeach();

var every = function(arr, fun){

    for(let i = 0; i < arr.length; i++){
        return arr[i] <5;
    }
    return true;
}